---
- name: Create a security group
  amazon.aws.ec2_security_group:  # Updated module name
    name: "{{ ec2_security_group_name }}"
    description: Allow all inbound traffic
    vpc_id: "{{ ec2_vpc_id }}"
    rules:
      - proto: tcp
        from_port: 0
        to_port: 65535
        cidr_ip: 0.0.0.0/0

- name: Provision EC2 instances
  amazon.aws.ec2_instance:  # Confirmed correct module name
    key_name: "{{ ec2_keypair }}"
    instance_type: "{{ ec2_instance_type }}"
    image_id: "{{ ec2_ami }}"
    region: "{{ ec2_region }}"
    vpc_subnet_id: "{{ ec2_subnet_id }}"
    count: 1
    security_group: "{{ ec2_security_group_name }}"
    tags:
      Name: "Ansible Provisioned EC2"
    network:
      assign_public_ip: true  # Use `true` instead of `yes`
